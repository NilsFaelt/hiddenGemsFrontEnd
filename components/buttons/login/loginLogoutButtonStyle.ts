import { StyleSheet } from "react-native";

export const logonLogoutButtonStyles = StyleSheet.create({
  container: {
    position: "absolute",
    flex: 0,
    alignItems: "center",
    justifyContent: "center",
    backgroundColor: "#fde349",
    right: 5,
    top: 35,
    padding: 15,
    borderRadius: 25,
    width: 90,
    Height: 45,
  },
});
