import { StyleSheet } from "react-native";

export const buttonStyles = StyleSheet.create({
  container: {
    width: 100,
    height: 50,
    backgroundColor: "white",
    shadowColor: "black",
    opacity: 0.9,
    margin: 10,
  },
});
